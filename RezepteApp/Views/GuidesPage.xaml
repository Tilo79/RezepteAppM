<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RezepteApp.Views.GuidesPage"
             Title="Guides"
             BackgroundColor="#FFFBFC">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            
            <!-- Page Header -->
            <Label Text="Koch-Guides &amp; Tipps"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="#4A4A4A"/>
            
            <Label Text="Hilfreiche Anleitungen und Kochtipps für Anfänger und Profis"
                   FontSize="18"
                   TextColor="#757575"
                   Margin="0,-10,0,10"/>

            <!-- Basic Cooking Techniques Card -->
            <Border BackgroundColor="{StaticResource CardPink}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}" Offset="0,4" Radius="15" Opacity="0.15"/>
                </Border.Shadow>
                
                <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto">
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            WidthRequest="70"
                            HeightRequest="70"
                            BackgroundColor="#FFB8C5"
                            VerticalOptions="Center"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15"/>
                        </Border.StrokeShape>
                        <Label Text="📖"
                               FontSize="36"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>

                    <Label Grid.Column="1" Grid.Row="0"
                           Text="Grundlagen des Kochens"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#4A4A4A"/>
                    
                    <Label Grid.Column="1" Grid.Row="1"
                           Text="Lernen Sie die wichtigsten Kochtechniken: Braten, Dünsten, Backen und mehr."
                           FontSize="13"
                           TextColor="#757575"
                           LineBreakMode="WordWrap"
                           Margin="0,5,0,0"/>
                    
                    <Label Grid.Column="1" Grid.Row="2"
                           Text="12 Lektionen • Anfänger"
                           FontSize="11"
                           TextColor="#9B9B9B"
                           Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <!-- Knife Skills Card -->
            <Border BackgroundColor="{StaticResource CardPeach}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}" Offset="0,4" Radius="15" Opacity="0.15"/>
                </Border.Shadow>
                
                <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto">
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            WidthRequest="70"
                            HeightRequest="70"
                            BackgroundColor="#FFD4A3"
                            VerticalOptions="Center"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15"/>
                        </Border.StrokeShape>
                        <Label Text="🔪"
                               FontSize="36"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>

                    <Label Grid.Column="1" Grid.Row="0"
                           Text="Messerkunde"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#4A4A4A"/>
                    
                    <Label Grid.Column="1" Grid.Row="1"
                           Text="Professionelle Schneidetechniken für Gemüse, Fleisch und Fisch."
                           FontSize="13"
                           TextColor="#757575"
                           LineBreakMode="WordWrap"
                           Margin="0,5,0,0"/>
                    
                    <Label Grid.Column="1" Grid.Row="2"
                           Text="8 Techniken • Mittel"
                           FontSize="11"
                           TextColor="#9B9B9B"
                           Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <!-- Seasoning Card -->
            <Border BackgroundColor="{StaticResource CardLavender}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}" Offset="0,4" Radius="15" Opacity="0.15"/>
                </Border.Shadow>
                
                <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto">
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            WidthRequest="70"
                            HeightRequest="70"
                            BackgroundColor="#D8C4E8"
                            VerticalOptions="Center"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15"/>
                        </Border.StrokeShape>
                        <Label Text="🧂"
                               FontSize="36"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>

                    <Label Grid.Column="1" Grid.Row="0"
                           Text="Gewürze &amp; Würzen"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#4A4A4A"/>
                    
                    <Label Grid.Column="1" Grid.Row="1"
                           Text="Die Kunst des richtigen Würzens: Timing, Dosierung und Kombinationen."
                           FontSize="13"
                           TextColor="#757575"
                           LineBreakMode="WordWrap"
                           Margin="0,5,0,0"/>
                    
                    <Label Grid.Column="1" Grid.Row="2"
                           Text="15 Gewürze • Alle Level"
                           FontSize="11"
                           TextColor="#9B9B9B"
                           Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <!-- Meal Planning Card -->
            <Border BackgroundColor="{StaticResource CardBeige}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}" Offset="0,4" Radius="15" Opacity="0.15"/>
                </Border.Shadow>
                
                <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto">
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            WidthRequest="70"
                            HeightRequest="70"
                            BackgroundColor="#F5E6D3"
                            VerticalOptions="Center"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15"/>
                        </Border.StrokeShape>
                        <Label Text="📅"
                               FontSize="36"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>

                    <Label Grid.Column="1" Grid.Row="0"
                           Text="Meal Prep Guide"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#4A4A4A"/>
                    
                    <Label Grid.Column="1" Grid.Row="1"
                           Text="Effizient vorkochen und die ganze Woche gesund essen."
                           FontSize="13"
                           TextColor="#757575"
                           LineBreakMode="WordWrap"
                           Margin="0,5,0,0"/>
                    
                    <Label Grid.Column="1" Grid.Row="2"
                           Text="Wochenplanung • Praktisch"
                           FontSize="11"
                           TextColor="#9B9B9B"
                           Margin="0,8,0,0"/>
                </Grid>
            </Border>

            <!-- Food Safety Card -->
            <Border BackgroundColor="{StaticResource CardMint}"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}" Offset="0,4" Radius="15" Opacity="0.15"/>
                </Border.Shadow>
                
                <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto">
                    <Border Grid.Row="0" Grid.RowSpan="3"
                            WidthRequest="70"
                            HeightRequest="70"
                            BackgroundColor="#C8F0E0"
                            VerticalOptions="Center"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15"/>
                        </Border.StrokeShape>
                        <Label Text="✓"
                               FontSize="36"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>

                    <Label Grid.Column="1" Grid.Row="0"
                           Text="Lebensmittelsicherheit"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#4A4A4A"/>
                    
                    <Label Grid.Column="1" Grid.Row="1"
                           Text="Richtige Lagerung, Haltbarkeit und sichere Zubereitung von Lebensmitteln."
                           FontSize="13"
                           TextColor="#757575"
                           LineBreakMode="WordWrap"
                           Margin="0,5,0,0"/>
                    
                    <Label Grid.Column="1" Grid.Row="2"
                           Text="Wichtiges Wissen • Essential"
                           FontSize="11"
                           TextColor="#9B9B9B"
                           Margin="0,8,0,0"/>
                </Grid>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
